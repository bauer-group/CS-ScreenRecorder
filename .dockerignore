# Git
.git
.gitignore
.gitattributes

# GitHub
.github

# Documentation
*.md
LICENSE

# IDE
.vscode
.idea
.claude

# Docker compose files (not needed in image, only Dockerfile)
docker-compose*.yml
.docker

# OS
.DS_Store
Thumbs.db

# Deployment configs (not needed in image)
.env
.env.*

# Tools container (separate image)
tools/

# Scripts (generate-assets runs locally, not in image)
scripts/

# Source branding files (EPS/SVG/PNG sources not needed, only generated assets)
src/branding/*.eps
src/branding/*.svg
src/branding/*.png
src/branding/branding.env

# Keep assets and scripts for Docker build
!src/branding/assets/
!src/branding/apply-branding.sh
!src/patches/
